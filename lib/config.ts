// Nesta fase inicial, cadastro de barbearia não é autosserviço — o dono do
// SaaS cria a conta diretamente (evita toda a fragilidade de confirmação por
// e-mail). Reative isso quando o fluxo de cadastro público voltar a fazer
// sentido (ex.: depois de verificação por WhatsApp).
export const SELF_SIGNUP_ENABLED = false;
